/*
For this quiz, use a jQuery tag selector to grab all of the <li>s on the page!
*/

// Start with this variable! (don't delete it!)
var listElements;

// Select every <li> element inside test.html
listElements = $("li");
console.log(listElements);